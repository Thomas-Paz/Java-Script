var nome = 'Thomas'; 
console.log (nome);

let nomeAluno; // undefined -> não aponta para local nenhum na memoria

const sobrenomeAluno = null; // Nulo -> não aponta para local nenhuma na memoria

const boolean = true; // or false; = const aprovado = true or false = valor "boolean" contém apenas 2 valores true ou false (lógico)

